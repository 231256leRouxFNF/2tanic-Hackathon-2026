/**
 * Future communication with the Arduino.
 *
 * This file can later:
 * - Read serial data
 * - Parse incoming messages
 * - Emit events to the frontend
 *
 * Placeholder for now.
 */

export function connectArduino(data) {
    console.log("Arduino connection event received:", data);
}